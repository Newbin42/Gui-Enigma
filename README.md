The source code requires pygame to run.

To install pygame on Windows:
  
  1. Open cmd prompt
  
  2. Run "pip install pygame"
    
linux/Mac:
  
  1. Open shell
    
  2. Run "pip install pygame"
    
Compiled with pygbag.
